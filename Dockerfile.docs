FROM squidfunk/mkdocs-material:9.5.18
LABEL org.opencontainers.image.source="crm-esl" \
      org.opencontainers.image.description="Pinned MkDocs Material image with linkcheck plugin" \
      maintainer="docs-bot <docs-bot@local>"

# Install required plugins (add more here as needed)
RUN pip install --no-cache-dir mkdocs-linkcheck==0.9.0

WORKDIR /docs
